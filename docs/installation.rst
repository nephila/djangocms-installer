============
Installation
============

At the command line::

    $ easy_install aldryn-installer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv aldryn-installer
    $ pip install aldryn-installer